<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    android:orientation="vertical">

    <!-- Function Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
             />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingBottom="6dp"
            android:paddingEnd="12dp"
            android:paddingStart="12dp"
            android:paddingTop="6dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/function"
                android:textColor="?attr/colorOnSurface"
                android:textSize="14sp" />

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
             />
    </LinearLayout>

    <TextView
        android:id="@+id/btn_new_search"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/start_new_search"
        android:clickable="true"
        android:focusable="true"
        android:padding="12dp"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        app:drawableStartCompat="@drawable/ic_search_mt"
        android:drawablePadding="16dp"
        app:drawableTint="?attr/colorOnSurface"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/btn_search_in_results"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/search_curr"
        android:clickable="true"
        android:focusable="true"
        android:padding="12dp"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        app:drawableStartCompat="@drawable/ic_search_mt"
        android:drawablePadding="16dp"
        app:drawableTint="?attr/colorOnSurface"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/btn_replace_in_results"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/rep_curr"
        android:clickable="true"
        android:focusable="true"
        android:padding="12dp"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        app:drawableStartCompat="@drawable/ic_search_replace"
        android:drawablePadding="16dp"
        app:drawableTint="?attr/colorOnSurface"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/btn_clear_results"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/clear_search_results"
        android:clickable="true"
        android:focusable="true"
        android:padding="12dp"
        android:background="?attr/selectableItemBackground"
        android:gravity="center_vertical"
        app:drawableStartCompat="@drawable/ic_close_mt"
        android:drawablePadding="16dp"
        app:drawableTint="?attr/colorOnSurface"
        android:textSize="16sp" />

    <LinearLayout
        android:id="@+id/layout_search_info"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone">

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
             />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingBottom="6dp"
            android:paddingEnd="12dp"
            android:paddingStart="12dp"
            android:paddingTop="6dp">

            <TextView
                android:id="@+id/tv_search_results_info"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:maxLines="3"
                android:textColor="?attr/colorOnSurface"
                android:textSize="14sp" />

            <ImageView
                android:id="@+id/btn_treeview_options"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_more_mt"
                app:tint="?attr/colorOnSurface" />

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1px"
             />
    </LinearLayout>

</LinearLayout>
